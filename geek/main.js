window.onload = function () {
    let rootSize = document.documentElement;
    console.log(rootSize.style);
    let allNode = document.querySelectorAll('body *');
    for(let i =0;i<allNode.length;i++) {
        if (allNode[i].getAttribute('font-size')) {}
        if (allNode[i].getAttribute('width')) {}
        if (allNode[i].getAttribute('font-size')) {}
        if (allNode[i].getAttribute('font-size')) {}
        if (allNode[i].getAttribute('font-size')) {}
    }    
}